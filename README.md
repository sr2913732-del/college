
  # HIMCS SWS

  This is a code bundle for HIMCS SWS. The original project is available at https://www.figma.com/design/o4WYSv6wPZo5DzXnVHcrpm/HIMCS-SWS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  